<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Quizinator</title>
</head>



{{!-- Top NavBar --}}
<div class="container flex flex-wrap justify-between items-center mx-auto">
{{!-- Page Title --}}
    <span
        class="self-center text-2xl font-semibold">Quizinator
        9000</span>
    <div class="flex space-x-4">
{{!-- Home button --}}
        <a href="/" class="bg-gray-900 text-white p-3 rounded-md text-md">Home</a>
{{!-- Dashboard button --}}
        <a href="/dashboard" class="bg-gray-900 text-white p-3 rounded-md text-md">Dashboard</a>
{{!-- If logged in show logout button --}}
        {{#if loggedIn}}
        <a href="#" id='logout-btn' class="bg-gray-900 text-white p-3 rounded-md text-md">Logout</a>
{{!-- If logged out show login button --}}
        {{else}}
        <a href="/login" class="bg-gray-900 text-white p-3 rounded-md text-md">Login</a>
        {{/if}}
    </div>
</div>

<div class="flex place-content-end">
    <main class="absolute w-3/4 right-20">
        {{{body}}}
    </main>
</div>

{{!-- Side nav bar for categories--}}
<div class="w-60 h-full shadow-md bg-white px-1 absolute">
    <ul class="relative">
        <li class="relative">
            <div class="flex items-center text-lg p-6 h-12 text-gray-700 rounded">Categories</div>
        </li>

        <li class="relative">
            <a class="flex items-center text-md p-6 h-10 text-gray-700 rounded hover:text-gray-900 hover:bg-gray-100 transition duration-400 ease-in-out"
                href="#!">Sci-Fi</a>
        </li>
        <li class="relative">
            <a class="flex items-center text-md p-6 h-10 text-gray-700 rounded hover:text-gray-900 hover:bg-gray-100 transition duration-400 ease-in-out"
                href="#!">Fantasy</a>
        </li>
        <li class="relative">
            <a class="flex items-center text-md p-6 h-10 text-gray-700 rounded hover:text-gray-900 hover:bg-gray-100 transition duration-400 ease-in-out"
                href="#!">Animals</a>
        </li>
        <li class="relative">
            <a class="flex items-center text-md p-6 h-10 text-gray-700 rounded hover:text-gray-900 hover:bg-gray-100 transition duration-400 ease-in-out"
                href="#!">Category 4</a>
        </li>
        <li class="relative">
            <a class="flex items-center text-md p-6 h-10 text-gray-700 rounded hover:text-gray-900 hover:bg-gray-100 transition duration-400 ease-in-out"
                href="#!">Category 5</a>
        </li>


    </ul>
</div>

<body>
    {{#if loggedIn}}
    <script src='/js/logout.js'></script>
    {{/if}}
</body>

</html>
